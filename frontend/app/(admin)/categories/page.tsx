import { DataTable } from "@/components/data-table";

export default function CategoriesPage() {
    return (
        <div>
            <DataTable
            columns={[]}
            data={[]}
            />
        </div>
    )
}